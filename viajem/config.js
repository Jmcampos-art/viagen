/* ============================================================
   AHGA TURISMO - CONFIGURAÇÃO DO FRONTEND
   ============================================================ */

// 🔧 LOCAL (desenvolvimento):
window.API_URL = 'http://localhost:3000';

// 🚀 PRODUÇÃO (Render):
// window.API_URL = 'https://ahga-turismo.onrender.com';

console.log('✅ config.js carregado. API_URL =', window.API_URL);